package solids;

import transforms.Point3D;

public class Arrow extends Solid {
    public Arrow() {

        // TODO: naplnit vertex a index buffer (souřadnice rasteru)
        vertexBuffer.add(new Point3D());
    }
}
